import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      description="Preparando o chat por texto e o fluxo de rascunho."
      listLabel="Carregando conversa"
      title="Falar com NEXIS"
    />
  );
}
